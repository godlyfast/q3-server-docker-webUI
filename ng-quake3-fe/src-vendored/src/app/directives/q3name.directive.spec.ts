import { Q3nameDirective } from './q3name.directive';

describe('Q3nameDirective', () => {
  it('should create an instance', () => {
    const directive = new Q3nameDirective();
    expect(directive).toBeTruthy();
  });
});
